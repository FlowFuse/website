# Listing metadata only. The page itself is pages/use-cases/scada.vue - this one
# had a real markup body in src/use-cases/scada.njk, so it did not lift into data the way the
# two operational use-cases did. Kept here so /use-cases/ still builds its cards from
# one place rather than a hand-maintained list.
slug: scada
title: "FlowFuse for SCADA"
problem: "Modernize SCADA with low-code flows that connect legacy hardware, stream real-time data, and drive dashboards and alerts from a single platform."
