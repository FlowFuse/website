# Listing metadata only. The page itself is pages/use-cases/remote-device-management.vue
# (formerly /use-cases/edge-connectivity/, which now redirects here). Its layout is a
# hand-written page rather than data, but it is kept here so /use-cases/ still builds its
# cards from one place rather than a hand-maintained list.
slug: remote-device-management
title: "Remote Device Management for Industrial Equipment"
problem: "FlowFuse runs Node-RED next to your PLCs and governs it from one place with remote device management, editing, snapshots, and fleet-wide controls."
