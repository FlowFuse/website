# Listing metadata only. The page itself is pages/use-cases/it-ot-middleware.vue - this one
# had a real markup body in src/use-cases/it-ot-middleware.njk, so it did not lift into data the way the
# two operational use-cases did. Kept here so /use-cases/ still builds its cards from
# one place rather than a hand-maintained list.
slug: it-ot-middleware
title: "FlowFuse for IT/OT Integration"
problem: "Bridge OT protocols and IT systems with low-code flows. Connect PLCs, SCADA, and historians to ERP, databases, and cloud services."
