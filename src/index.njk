---
layout: default
sitemapPriority: 1.0
---
{% include "snow-js.njk" %}
<!--Hero Content-->
<div id="snow" class="w-full pt-9 mt-3">
    <div class="w-full px-6 pb-24 md:px-0 ff-bg-flows">
        <div class="container m-auto text-center max-w-2xl">
            <h1 class="text-gray-50 max-w-xl m-auto">
                Build bespoke, flexible, and resilient manufacturing applications
            </h1>
            <p class="mt-6 text-gray-400 text-lg font-normal">
                FlowFuse and Node-RED allows your engineers to deliver<br />custom manufacturing applications that can grow to meet your changing requirements.
            </p>
            <div class="flex flex-col">
                <div class="m-auto mt-7 flex flex-col gap-4 items-center justify-center button-container">
                    <a class="ff-btn ff-btn--primary flex flex-col" href="{{ site.appURL }}/account/create" onclick="capture('cta-join', {'position': 'primary'})">
                        <span class="text-base uppercase flex gap-2 items-center">
                            Try it out
                            {% include "components/icons/arrow-right.svg" %}
                        </span>
                    </a>
                    <img class="corner-icon" src="../images/christmas-hat.png" alt="Christmas hat">
                </div>
            </div>
        </div>
    </div>
</div>

{% include "components/divider-flow--default.njk" %}

<div class="flex justify-center">
    <lite-youtube videoid="eR0P40sxge8" style="background-image: url('images/thumbnail-nick-flowfuse.png'); width: 620px; height: 360px" class="absolute -mt-10 max-w-xs max-h-48 border-4 rounded-md sm:max-w-none sm:max-h-full"></lite-youtube>
</div>

<!-- Secondary Content -->
<div class="w-full ff-bg-light px-6 pt-12 sm:pt-32">
    <div class="container m-auto text-center max-w-5xl">
        <div class="m-auto mb-12 w-full text-center mt-32 mb-12 sm:mt-56 sm:mb-16">
            {% include "social-proof.njk" %}
        </div>
        <div class="m-auto w-full text-center mb-8 md:mt-24">
            <h2>Why FlowFuse and Node-RED</h2>
            <h5><a href="/node-red/">Learn more about Node-RED</a></h5>
        </div>
    </div>
</div>
<div class="about w-full ff-bg-light -mt-1 pb-40">
    <div class="container m-auto max-w-lg md:max-w-5xl flex flex-col gap-y-4">
        {% for item in home %}
            <div class="relative w-full p-6 m-auto">
                <div class="flex flex-col md:flex-row{% if loop.index is even %}-reverse{% endif %} md:gap-8 items-center z-10 px-8 md:justify-between">
                    <div class="md:w-1/4 flex justify-center">
                        <div class="max-w-[190px] w-full max-w-sm md:max-w-none sm:max-h-full">
                            {% set imageSrc = ['./images/pictograms/', item.image, '.png'] | join %}
                            {% set imageDescription = ['Image depicting ', item.imagealt] | join %}
                            {% image imageSrc, imageDescription, [190] %}
                        </div>                      
                    </div>
                    <div class="md:w-3/4 pt-4">
                        <div class="flex flex-row gap-3 items-center">
                            <h3><div class="leading-tight pt-1">{{ item.title | safe }}</div></h3>
                        </div>
                        <p>{{ item.content }}</p>
                    </div>
                </div>
            </div>
            {% if not loop.last %}
            <div class="md:p-6 px-6">
            {% if loop.index is even %}{% include "components/divider-grey-wires-left.njk" %}{% else %}{% include "components/divider-grey-wires-right.njk" %}{% endif %} 
            </div>
            {% endif %}
        {% endfor %}
    </div>
</div>

{% include "components/divider-flow--top-red--dark.njk" %}

<!-- Deployment Options -->
<div class="w-full ff-bg-dark px-6 pt-0 pb-32 md:pt-0 md:px-0">
    <div class="container m-auto text-center max-w-4xl">
        <div class="m-auto mt-16 w-full text-center">
            <h2>Get Started with FlowFuse</h2>
            <p class="text-white font-light">Explore our <a href="/pricing/">pricing</a>.</p>
        </div>
        <div class="sm:mt-10 md:mt-10 w-full max-w-4xl text-center flex flex-wrap gap-2 justify-around">
            <div class="w-full px-3 mt-4 sm:w-72 md:mt-0 css-full">
                <div class="w-40 m-auto mb-4">
                    {% image "./images/pictograms/book-a-demo.png", "Icon representing a booked demo.", [160] %}
                </div>
                <h5 class="flex justify-center">Talk to an Expert</h5>
                <label class="font-light mt-2 block">The best way to learn about FlowFuse is to book a demo! We’d love to show you how FlowFuse can transform your business.</label>
                <a class="mt-4 ff-btn ff-btn--secondary uppercase inline-block" href="/book-demo/" onclick="capture('cta-talk-to-expert', {'position': 'secondary'})">Book a Demo</a>
            </div>
            <div class="w-full px-3 mt-4 sm:w-72 md:mt-0 flex flex-col items-center justify-between">
                <div>
                <div class="w-40 mb-4">
                    {% image "./images/pictograms/enterprise.png", "Graphic image representing 'Infrastructure'.", [160] %}
                </div>
                <h5 class="flex justify-center">Get Started Today</h5>
                </div>
                <label class="font-light mt-2 block">Ready to get started? Start exploring the FlowFuse platform in less than a minute.</label>
                <a class="mt-4 ff-btn ff-btn--primary uppercase inline-block" href="{{ site.appURL }}/account/create" onclick="capture('cta-get-started-today', {'position': 'secondary'})">Join Now</a>   
            </div>
        </div>
    </div>
</div>
