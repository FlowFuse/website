---
pagination:
    data: coreNodes.sequence
    size: 1
    alias: node
    addAllPagesToCollections: true
coreNodeCategory: "sequence"
permalink: "node-red/core-nodes/{{ node.name | lower | replace(\" \", \"\") }}/"
eleventyComputed:
  navTitle: "{{ node.name }}"
  navGroup: "{{ coreNodeCategory }}"
---

{% include "core-node-docs.njk" %}
