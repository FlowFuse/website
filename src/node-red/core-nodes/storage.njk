---
pagination:
    data: coreNodes.storage
    size: 1
    alias: node
    addAllPagesToCollections: true
coreNodeCategory: "storage"
permalink: "node-red/core-nodes/{{ node | coreNodeName }}/"
eleventyComputed:
  meta:
    title: "{{ node | coreNodeName }}"
  navTitle: "{{ node | coreNodeName }}"
  navGroup: "{{ coreNodeCategory }}"
---

{% include "core-node-docs.njk" %}
