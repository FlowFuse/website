---
# The building materials industry page itself lives in Nuxt:
# nuxt/pages/industries/building-materials.vue. It was built there so it could embed
# the ROI calculator, which is a Vue component (see the comment at the top of that file).
#
# This stub stays behind for one reason: the card grid on /industries/
# (src/industries.njk) is still 11ty and builds itself from the `industry`
# collection, so the entry below is what keeps building materials on that page.
#   permalink: false  - 11ty writes no HTML, so nothing lands in nuxt/public/ to
#                       shadow the Nuxt route, but the page stays in collections.
#   migratedUrl       - `url` is false under `permalink: false`; src/industries.njk
#                       reads this instead when it's set.
#   skipIndex         - keeps it out of sitemap-legacy.xml; Nuxt's own sitemap
#                       picks the real page up from its static route.
# Delete this file once /industries/ itself is migrated to Nuxt.
permalink: false
migratedUrl: /industries/building-materials/
skipIndex: true
meta:
  title: "Building Materials Manufacturing Applications | FlowFuse"
  description: "Connect extrusion lines, presses, CNC, glass lines, and plant IT systems in one place. FlowFuse helps building products manufacturers monitor, automate, and standardize production across plants in real time."
hero:
  # TODO(image): placeholder. Replace with a building products plant photo at
  # src/images/industries/building-materials.jpg, matching the automotive pattern.
  image: "/images/stories/large-us-manufacturing.jpg"
  imageAlt: "Production floor at a large manufacturing plant"
---
