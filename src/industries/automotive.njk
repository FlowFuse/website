---
# The automotive industry page itself now lives in Nuxt:
# nuxt/pages/industries/automotive.vue. It moved there so it could embed the ROI
# calculator, which is a Vue component (see the comment at the top of that file).
#
# This stub stays behind for one reason: the card grid on /industries/
# (src/industries.njk) is still 11ty and builds itself from the `industry`
# collection, so the entry below is what keeps automotive on that page.
#   permalink: false  - 11ty writes no HTML, so nothing lands in nuxt/public/ to
#                       shadow the Nuxt route, but the page stays in collections.
#   migratedUrl       - `url` is false under `permalink: false`; src/industries.njk
#                       reads this instead when it's set.
#   skipIndex         - keeps it out of sitemap-legacy.xml; Nuxt's own sitemap
#                       picks the real page up from its static route.
# Delete this file once /industries/ itself is migrated to Nuxt.
permalink: false
migratedUrl: /industries/automotive/
skipIndex: true
meta:
  title: "Automotive Manufacturing Applications | FlowFuse"
  description: "Connect PLCs, SCADA, MES, and plant IT systems in one place. FlowFuse helps teams monitor, automate, and standardize production workflows across plants in real time."
hero:
  image: "/images/industries/automotive.jpg"
  imageAlt: "Aerial view of an automotive manufacturing plant floor"
---
