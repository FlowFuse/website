<div class="cta">
  <p>
    {% if cta_text and cta_text != "" %}
      {{ cta_text }}
    {% else %}
      Optimize your factory operations with FlowFuse. Effortlessly manage, deploy, scale, and secure your Node-RED instances.
    {% endif %}
    <a href="{{ cta_url }}" style="color: #2563eb; text-decoration: none; font-weight: semi-bold;">
      {% if cta_type == "talk" %}
        Talk to Us Now
      {% elsif cta_type == "demo" %}
        Let’s Walk You Through It – Book a Demo Today
      {% else %}
        Sign Up for FlowFuse Now
      {% endif %}
    </a>
  </p>
</div>
