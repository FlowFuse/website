<div class="ff-blog container m-auto text-left max-w-4xl pt-8 pb-24 w-full">
    <div class="px-2 flex items-center gap-12">
        <h1 class="mb-0">Blog</h1>
    </div>
    <div class="w-full px-2 my-4 grid">
        <div id="pagefind-search" class="border rounded w-full justify-self-end"></div>
    </div>
    <div class="px-2 my-4 flex flex-wrap flex-row gap-4 md:justify-between">
        {%- for tag in blogTags -%}
            {% if tag.value !== "posts" %}
            <a class="hover:no-underline" href="/blog/{{ tag.value }}/"><button class="w-full ff-btn ff-btn--primary{% if not page.url.includes('/' + tag.value + '/')%}-outlined{% endif %}">{{ tag.label }}</button></a>
            {% else %}
            <a class="hover:no-underline" href="/blog/"><button class="w-full ff-btn ff-btn--primary{% if page.url !== "/blog/"%}-outlined{% endif %}">{{ tag.label }}</button></a>
            {% endif %}
        {% endfor %}
    </div>
    <ul class="flex flex-wrap">
        {%- asyncEach item in posts -%}
            {% include "blog/blog-posts.njk" %}
        {%- endeach -%}
    </ul>
    {% include "blog/pagination.njk" %}
</div>
{% include "layouts/common-js.njk" %}
