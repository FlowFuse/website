---
layout: layouts/base.njk
---
<!--Hero Content-->
<div class="w-full page">
    <div class="hero container m-auto text-center max-w-4xl flex mb-32">
        <div class="text-left">
            <h1 class="text-4xl">{{ title }}</h1>
            <h4>{{ subtitle | safe }}</h4>
            <div>{{ description | safe }}</div>
        </div>
        <img src="{{heroimg}}"/>
    </div>
    <div class="content">
        {{ content | safe }}
    </div>
</div>