---
title: Meet the Team
layout: layouts/nohero.njk
---
<div class="team w-full pb-20">
    <div class="container m-auto text-left max-w-4xl">
        <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 md:grid-cols-3">
            {%- for member in team | dictsortBy(false, 'order') -%}
            <div id="{{ member._key }}" class="team-headshot w-full max-w-xs m-auto">
                {% set imageSrc = ["./images/team/headshot-", member.headshot ] | join %}
                {% set imageAlt = ["Photo of ", member.name ] | join %}
                {% image imageSrc, imageAlt, [256] %}
                <ul class="member-facts">
                    {%- for fact in member.facts -%}
                    <li>{{ fact | safe }}</li>
                    {%- endfor -%}
                </ul>
                <div class="member-info">
                    <div class="title">
                        <div class="name">{{ member.name }}</div>
                        <div class="role">{{ member.title }}</div>
                    </div>
                    <div class="socials">
                        {% if member.email %}
                        <span class="">
                            <a href="mailto:{{ member.email }}">
                                {% include "components/mail.njk" %}
                            </a>
                        </span>
                        {% endif %}
                        {% if member.twitter %}
                        <span class="">
                            <a class="" target="_blank" href="https://twitter.com/{{ member.twitter }}">
                                {% include "components/twitter.njk" %}
                            </a>
                        </span>
                        {% endif %}
                        {% if member.github %}
                        <span class="">
                            <a class="" target="_blank" href="https://github.com/{{ member.github }}">
                                {% include "components/github.njk" %}
                            </a>
                        </span>
                        {% endif %}
                        {% if member.linkedin %}
                        <span class="">
                            <a class="" target="_blank" href="https://www.linkedin.com/in/{{ member.linkedin }}">
                                {% include "components/linkedin.njk" %}
                            </a>
                        </span>
                        {% endif %}
                        {% if member.blog %}
                        <span class="">
                            <a class="" target="_blank" href="{{ member.blog }}">
                                {% include "components/rss.njk" %}
                            </a>
                        </span>
                        {% endif %}
                    </div>
                </div>
            </div>
            {%- endfor -%}
        </div>
    </div>
</div>

<!-- Benefits -->
<div class="about w-full bg-gray-50 px-6 pt-12 pb-12 md:pt-20 md:px-0">
    <div class="container m-auto text-center max-w-4xl">
        <div>
            <h2 class="mb-4">Working at FlowFuse</h2>
            <p>Here are just some of the benefits enjoyed by our employees. We are always looking to improve and expand these too. You can read more about them in our <a href="/handbook/peopleops/">Handbook.</a></p>
        </div>
        {% include "benefits.njk" %}
    </div>
</div>

<!-- Company Values -->
<div class="about w-full px-6 pt-12 pb-20 md:px-0 md:pt-20">
    <div class="container m-auto text-center max-w-4xl">
        <div class="">
            <h1 class="mb-4">Company Values</h1>
            <p>We are full open about our company values, you can read more about them in our <a href="/handbook/company/">Handbook.</a></p>
        </div>
        {% include "values.njk" %}
        <p>Interested in joining FlowFuse? Check out our <a href="{{ site.jobBoard }}">Jobs</a> page for our current openings.</p>
    </div>
</div>
